ext {
//    globalVersion = ""
//
//    projectVersions = [:]
//    subprojects {
//        if (rootProject.hasProperty("${project.name}Version")) {
//            projectVersions."${project}" = "${project.name}Version"
//        } else {
//            projectVersions."${project}" = globalVersion
//        }
//    }
    //projectVersions.each() { key, value -> println "${key} == ${value.getProjectVersion()}" };
}